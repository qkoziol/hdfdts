# Generic Makefile to build the IO speed measurement tool.

CPPFLAGS=-D_LARGEFILE_SOURCE -D_LARGEFILE64_SOURCE -D_FILE_OFFSET_BITS=64

iospeed: gopt.c iospeed.c
	$(CC) $(CPPFLAGS) gopt.c iospeed.c -o $@

clean:
	rm -f iospeed
